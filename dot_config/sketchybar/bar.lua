local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 40,
  shadow = true,
  sticky = true,
  padding_right = 10,
  padding_left = 10,
  blur_radius=30,
  topmost="window",
})
